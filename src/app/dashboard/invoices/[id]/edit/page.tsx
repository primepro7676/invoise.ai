import { notFound } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { InvoiceForm } from "@/components/invoice/invoice-form";
export default async function EditInvoicePage({params}:{params:Promise<{id:string}>}){const {id}=await params; const invoice=await prisma.invoice.findUnique({where:{id},include:{customer:true,lineItems:{orderBy:{sortOrder:"asc"}}}}); if(!invoice)notFound(); const initial={invoiceNumber:invoice.invoiceNumber,invoiceDate:invoice.invoiceDate.toISOString().slice(0,10),dueDate:invoice.dueDate.toISOString().slice(0,10),paymentStatus:invoice.paymentStatus,lineItems:invoice.lineItems.map(li=>({categoryName:li.categoryName,packageName:li.packageName,description:li.description,quantity:li.quantity,rate:li.rate,isCustomPrice:li.isCustomPrice,discount:li.discount,gstPercent:li.gstPercent})),gstEnabled:invoice.gstEnabled,gstPercent:invoice.gstPercent,amountPaid:invoice.amountPaid,paymentMethod:invoice.paymentMethod,upiId:invoice.upiId,transactionRef:invoice.transactionRef,notes:invoice.notes,customerId:invoice.customerId}; return <div className="space-y-6"><div><h1 className="text-2xl font-semibold text-navy-900">Edit Invoice</h1><p className="text-sm text-navy-600/70">Update invoice details, payment and services.</p></div><InvoiceForm mode="edit" invoiceId={id} initialValues={initial}/></div>}
